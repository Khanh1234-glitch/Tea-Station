export class siteStat {
    products: number;
    customers: number;
    rating: number;
    constructor(products: number, customer: number, rating: number) {
        this.products = products;
        this.customers = customer;
        this.rating = rating;
    }
}
